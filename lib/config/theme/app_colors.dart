import 'package:flutter/material.dart';

class AppColors {
  // ===============================
  // BRAND / PRIMARY
  // ===============================
  static const Color primary = Color(0xFF624731); // coklat utama

  // ===============================
  // BACKGROUND & SURFACE
  // ===============================
  static const Color background = Color(0xFFFFFFFF);
  static const Color card = Color(0xFFF2F2F2);
  static const Color border = Color(0xFFDDDDDD);

  // ===============================
  // TEXT
  // ===============================
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF797979);

  // ===============================
  // STATUS COLORS
  // ===============================
  static const Color success = Color(0xFF19A800); // On Time
  static const Color danger = Color(0xFFE90000); // Late / Error
  static const Color warning = Color(0xFFFEDB5E);
}
