<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- PERMISSION WAJIB APLIKASI ABSENSI -->

    <!-- Lokasi -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Internet & Network -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Kamera (WAJIB untuk selfie check-in / check-out) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Untuk MapView lebih akurat -->
    <uses-feature android:name="android.hardware.location.gps" />

    <!-- Untuk mengambil foto selfie -->
    <uses-feature android:name="android.hardware.camera.any" />
    <uses-feature android:name="android.hardware.camera.front" android:required="false" />


    <application
        android:label="absensi_karyawan_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- GOOGLE MAPS API KEY -->

        <!-- dibiarkan kosong sementara -->
         
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="PASTE_API_KEY_MU_DI_SINI" />


        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Flutter Theme -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <!-- Flutter Auto Generate -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>

    <!-- Android 11+ package visibility rules -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>
